export type AuthFormProps = {
  title: string;
  description: string;
  endpoint: string;
  redirectLink: string;
  buttonLabel: string;
  question: string;
  answer: string;
  redirectionLink: string;
};
